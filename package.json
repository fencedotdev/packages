{
  "name": "packages",
  "private": true,
  "version": "0.0.0",
  "description": "Public, versioned packages Fence publishes for third-party consumption and cross-repo reuse — the contracts schemas, and did:web/credential-verification logic. See CLAUDE.md for the public/private split rationale (internal-only shared code lives in a separate, private repo).",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/contracts && npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint . --max-warnings=0",
    "check:banned-folders": "bash scripts/check-banned-folders.sh",
    "check:payment-fields": "bash scripts/check-payment-fields.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.0.0",
    "eslint": "^10.9.1",
    "eslint-plugin-security": "^4.0.1",
    "husky": "^9.1.7",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.0.0"
  }
}
